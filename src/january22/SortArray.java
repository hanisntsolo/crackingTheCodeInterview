package january22;

import java.util.Arrays;
class SortArray {
    public int[] sortArray(int[] nums) {
        //Automatic methods for sorting the array!
        Arrays.sort(nums);
        return nums;
        //Custom logic
        //possibly using bubble sort
    }
}
